<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Instagram Clone</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Google Fonts: Inter & Billabong-like Cursive for Logo -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Grand+Hotel&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <!-- Lucide Icons -->
  <script src="https://unpkg.com/lucide@latest"></script>
  <style>
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      background-color: #000000;
      color: #000000;
      user-select: none;
      -webkit-tap-highlight-color: transparent;
    }
    
    .font-logo {
      font-family: 'Grand Hotel', cursive;
    }

    /* Hide scrollbars for clean story bar */
    .no-scrollbar::-webkit-scrollbar {
      display: none;
    }
    .no-scrollbar {
      -ms-overflow-style: none;
      scrollbar-width: none;
    }

    /* Gradient border for Instagram Stories */
    .story-ring {
      background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
      padding: 2.5px;
      border-radius: 50%;
    }

    .story-ring-seen {
      background: #dbdbdb;
      padding: 2px;
      border-radius: 50%;
    }

    /* Heart animation effect */
    @keyframes heartPop {
      0% { transform: scale(0); opacity: 0; }
      50% { transform: scale(1.3); opacity: 0.9; }
      100% { transform: scale(1); opacity: 0; }
    }
    .animate-heart-pop {
      animation: heartPop 0.8s ease-in-out forwards;
    }

    /* Story timer progress bar */
    @keyframes storyProgress {
      0% { width: 0%; }
      100% { width: 100%; }
    }
    .animate-progress {
      animation: storyProgress 5s linear forwards;
    }

    /* Reels Transition */
    .reel-transition {
      transition: transform 0.35s cubic-bezier(0.25, 1, 0.5, 1);
    }
  </style>
</head>
<body class="flex justify-center items-center min-h-screen bg-neutral-900 md:py-6">

  <!-- Mobile Device Container frame for desktop / full width for phone -->
  <div class="w-full max-w-[430px] h-[100vh] md:h-[880px] bg-white text-black flex flex-col relative md:rounded-[40px] md:shadow-2xl overflow-hidden border border-neutral-800">

    <!-- INSTAGRAM SPLASH LOADER SCREEN -->
    <div id="splash-screen" class="absolute inset-0 z-50 bg-black flex flex-col justify-between items-center py-12 transition-opacity duration-500 text-white pointer-events-auto">
      <div></div>
      <!-- Center IG Gradient Icon -->
      <div class="flex flex-col items-center space-y-3 animate-pulse">
        <div class="w-20 h-20 rounded-3xl bg-gradient-to-tr from-amber-400 via-rose-500 to-purple-600 p-[3px] shadow-xl">
          <div class="w-full h-full bg-black rounded-[21px] flex items-center justify-center">
            <i data-lucide="instagram" class="w-10 h-10 text-white"></i>
          </div>
        </div>
        <span class="font-logo text-4xl font-bold tracking-wide">Instagram</span>
      </div>
      <!-- Footer: powered by html_codee -->
      <div class="flex flex-col items-center space-y-1">
        <span class="text-[11px] text-gray-500 uppercase tracking-widest font-semibold">powered by</span>
        <a href="https://t.me/html_codee" target="_blank" class="text-sm font-bold bg-gradient-to-r from-purple-400 via-pink-500 to-rose-400 bg-clip-text text-transparent hover:underline">
          Html_codee
        </a>
      </div>
    </div>

    <!-- MAIN APP CONTAINER -->
    <div class="flex-1 flex flex-col overflow-hidden relative">

      <!-- TOP HEADER BAR -->
      <header id="main-header" class="w-full bg-white border-b border-gray-100 px-4 py-2 flex justify-between items-center z-20 relative">
        <!-- Logo with Dropdown trigger -->
        <div class="relative">
          <button id="logo-dropdown-btn" onclick="toggleDropdown()" class="flex items-center space-x-1 focus:outline-none group active:opacity-70 transition">
            <span class="font-logo text-3xl font-bold tracking-wide leading-none pt-1">Instagram</span>
            <i data-lucide="chevron-down" class="w-4 h-4 text-black transition-transform duration-200" id="dropdown-chevron"></i>
          </button>

          <!-- DROPDOWN MENU -->
          <div id="dropdown-menu" class="hidden absolute top-10 left-0 bg-white shadow-xl rounded-2xl border border-gray-100 w-52 py-1 z-50 transition-all duration-200 transform opacity-0 scale-95">
            <button onclick="filterFeed('following')" class="w-full px-4 py-3 flex items-center justify-between text-sm font-semibold text-gray-800 hover:bg-gray-50 active:bg-gray-100 transition">
              <span>Following</span>
              <i data-lucide="users" class="w-5 h-5 text-gray-700"></i>
            </button>
            <button onclick="filterFeed('favorites')" class="w-full px-4 py-3 flex items-center justify-between text-sm font-semibold text-gray-800 hover:bg-gray-50 active:bg-gray-100 transition">
              <span>Favorites</span>
              <i data-lucide="star" class="w-5 h-5 text-gray-700"></i>
            </button>
            <button onclick="filterFeed('meta_verified')" class="w-full px-4 py-3 flex items-center justify-between text-sm font-semibold text-gray-800 hover:bg-gray-50 active:bg-gray-100 transition border-t border-gray-100">
              <span>Meta Verified</span>
              <i data-lucide="badge-check" class="w-5 h-5 text-blue-500 fill-blue-500/10"></i>
            </button>
          </div>
        </div>

        <!-- Right action icons (Gemini AI, Notifications & Direct Messages) -->
        <div class="flex items-center space-x-3.5">
          <button onclick="openGeminiModal()" class="relative active:scale-90 transition p-1.5 bg-gradient-to-tr from-purple-600 via-pink-500 to-amber-400 rounded-full text-white shadow-sm hover:shadow-md" title="Gemini AI Assistant">
            <i data-lucide="sparkles" class="w-4 h-4"></i>
          </button>
          <button onclick="openNotifications()" class="relative active:scale-90 transition">
            <i data-lucide="heart" class="w-6 h-6 stroke-[1.8]"></i>
            <span class="absolute top-0 right-0 w-2 h-2 bg-red-500 rounded-full animate-ping"></span>
            <span class="absolute top-0 right-0 w-2 h-2 bg-red-500 rounded-full"></span>
          </button>
          <button onclick="openMessages()" class="relative active:scale-90 transition">
            <i data-lucide="message-circle" class="w-6 h-6 stroke-[1.8] -rotate-12"></i>
            <span class="absolute -top-1 -right-1 bg-red-500 text-white text-[10px] font-bold px-1.5 py-0.2 rounded-full border-2 border-white">3</span>
          </button>
        </div>
      </header>

      <!-- VIEW PAGES SWITCHER CONTAINER -->
      <main class="flex-1 overflow-y-auto no-scrollbar relative bg-white" id="main-content">
        
        <!-- ================= FEED VIEW (TAB 1) ================= -->
        <div id="view-home" class="page-view block pb-16">
          
          <!-- Filter Indicator Tag -->
          <div id="filter-banner" class="hidden bg-gray-100 text-xs text-gray-600 px-4 py-1.5 flex items-center justify-between">
            <span id="filter-banner-text" class="font-medium">Showing: Following</span>
            <button onclick="filterFeed('all')" class="text-blue-600 font-semibold text-[11px]">Clear Filter</button>
          </div>

          <!-- STORIES HORIZONTAL SCROLL BAR -->
          <div class="border-b border-gray-100 py-2.5 px-3">
            <div class="flex space-x-4 overflow-x-auto no-scrollbar items-center" id="stories-container">
              <!-- Dynamically populated -->
            </div>
          </div>

          <!-- POSTS FEED CONTAINER -->
          <div id="posts-feed" class="divide-y divide-gray-100">
            <!-- Dynamic Post Items will load here -->
          </div>
        </div>

        <!-- ================= SEARCH / EXPLORE VIEW (TAB 2) ================= -->
        <div id="view-search" class="page-view hidden p-3 pb-16">
          <div class="sticky top-0 bg-white z-10 pb-3">
            <div class="bg-gray-100 rounded-xl px-3 py-2 flex items-center space-x-2">
              <i data-lucide="search" class="w-4 h-4 text-gray-400"></i>
              <input type="text" placeholder="Search" class="bg-transparent text-sm w-full outline-none text-gray-800 placeholder-gray-400" />
            </div>
          </div>
          <div class="grid grid-cols-3 gap-1 rounded-lg overflow-hidden" id="explore-grid">
            <!-- Populated dynamically -->
          </div>
        </div>

        <!-- ================= REELS VIEW (TAB 4 - SWIPEABLE) ================= -->
        <div id="view-reels" class="page-view hidden bg-black h-full relative text-white pb-16 overflow-hidden select-none">
          <div class="absolute top-4 left-4 z-20 font-bold text-xl drop-shadow-md flex items-center space-x-2">
            <span>Reels</span>
            <span class="text-[10px] bg-white/20 px-2 py-0.5 rounded-full text-white/90 font-normal backdrop-blur-md">Swipe Up / Down</span>
          </div>

          <!-- Reels Wrapper for touch dragging -->
          <div id="reels-wrapper" class="w-full h-full relative reel-transition" ontouchstart="handleReelTouchStart(event)" ontouchend="handleReelTouchEnd(event)">
            <!-- Dynamic Reel items inserted here -->
          </div>

          <!-- Vertical Reel Swipe Controls (Desktop / Touch alternative) -->
          <div class="absolute left-3 bottom-24 z-20 flex flex-col space-y-2">
            <button onclick="prevReel()" class="p-2 bg-black/40 rounded-full text-white backdrop-blur-md hover:bg-black/60 transition"><i data-lucide="chevron-up" class="w-5 h-5"></i></button>
            <button onclick="nextReel()" class="p-2 bg-black/40 rounded-full text-white backdrop-blur-md hover:bg-black/60 transition"><i data-lucide="chevron-down" class="w-5 h-5"></i></button>
          </div>
        </div>

        <!-- ================= PROFILE VIEW (TAB 5) ================= -->
        <div id="view-profile" class="page-view hidden pb-16">
          <!-- Profile Header -->
          <div class="p-4 border-b border-gray-100">
            <div class="flex items-center justify-between mb-4">
              <div class="flex items-center space-x-1">
                <span class="font-bold text-lg">your_handle</span>
                <i data-lucide="chevron-down" class="w-4 h-4"></i>
              </div>
              <div class="flex items-center space-x-4">
                <button onclick="openCreatePost()"><i data-lucide="plus-square" class="w-6 h-6"></i></button>
                <i data-lucide="menu" class="w-6 h-6"></i>
              </div>
            </div>

            <div class="flex items-center justify-between mb-4">
              <div class="relative">
                <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=200&q=80" class="w-20 h-20 rounded-full object-cover border border-gray-200" />
              </div>
              <div class="flex space-x-6 text-center">
                <div>
                  <div class="font-bold text-base">12</div>
                  <div class="text-xs text-gray-500">Posts</div>
                </div>
                <div>
                  <div class="font-bold text-base">1,240</div>
                  <div class="text-xs text-gray-500">Followers</div>
                </div>
                <div>
                  <div class="font-bold text-base">482</div>
                  <div class="text-xs text-gray-500">Following</div>
                </div>
              </div>
            </div>

            <div class="space-y-0.5 text-sm mb-4">
              <div class="font-semibold">Alex Rivera</div>
              <div class="text-gray-600">Digital Creator & Photographer 📸</div>
              <div class="text-gray-600">Exploring the world one lens at a time 🌍</div>
              <a href="#" class="text-blue-900 font-medium hover:underline text-xs block">linktr.ee/alexrivera</a>
            </div>

            <div class="flex space-x-2">
              <button class="flex-1 bg-gray-100 py-1.5 rounded-lg text-sm font-semibold active:bg-gray-200 transition">Edit profile</button>
              <button class="flex-1 bg-gray-100 py-1.5 rounded-lg text-sm font-semibold active:bg-gray-200 transition">Share profile</button>
              <button class="bg-gray-100 p-1.5 rounded-lg active:bg-gray-200 transition">
                <i data-lucide="user-plus" class="w-4 h-4"></i>
              </button>
            </div>
          </div>

          <div class="flex border-b border-gray-100 text-gray-400">
            <button class="flex-1 py-2.5 flex justify-center text-black border-b-2 border-black">
              <i data-lucide="grid" class="w-5 h-5"></i>
            </button>
            <button class="flex-1 py-2.5 flex justify-center hover:text-black">
              <i data-lucide="clapperboard" class="w-5 h-5"></i>
            </button>
            <button class="flex-1 py-2.5 flex justify-center hover:text-black">
              <i data-lucide="user-square-2" class="w-5 h-5"></i>
            </button>
          </div>

          <div class="grid grid-cols-3 gap-0.5" id="profile-grid">
            <!-- Loaded dynamically -->
          </div>
        </div>

      </main>

      <!-- BOTTOM NAVIGATION BAR -->
      <nav class="w-full bg-white border-t border-gray-100 px-6 py-2.5 flex justify-between items-center z-20 absolute bottom-0 left-0 right-0">
        <button onclick="switchTab('home')" class="nav-btn active text-black active:scale-90 transition">
          <i data-lucide="home" class="w-6 h-6 stroke-[2] fill-current"></i>
        </button>
        <button onclick="switchTab('search')" class="nav-btn text-gray-700 active:scale-90 transition">
          <i data-lucide="search" class="w-6 h-6 stroke-[2]"></i>
        </button>
        <button onclick="openCreatePost()" class="nav-btn text-gray-700 active:scale-90 transition">
          <i data-lucide="plus-square" class="w-6 h-6 stroke-[2]"></i>
        </button>
        <button onclick="switchTab('reels')" class="nav-btn text-gray-700 active:scale-90 transition">
          <i data-lucide="clapperboard" class="w-6 h-6 stroke-[2]"></i>
        </button>
        <button onclick="switchTab('profile')" class="nav-btn text-gray-700 active:scale-90 transition">
          <div class="w-6 h-6 rounded-full overflow-hidden border border-gray-300">
            <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=100&q=80" class="w-full h-full object-cover" />
          </div>
        </button>
      </nav>

    </div>

    <!-- GEMINI AI ASSISTANT MODAL -->
    <div id="gemini-modal" class="hidden fixed inset-0 z-50 bg-black/60 backdrop-blur-sm flex items-end md:items-center justify-center">
      <div class="bg-white w-full max-w-md h-[80%] md:h-[620px] rounded-t-3xl md:rounded-3xl flex flex-col p-4 animate-in slide-in-from-bottom duration-200">
        <!-- Header -->
        <div class="flex items-center justify-between border-b pb-3">
          <div class="flex items-center space-x-2">
            <div class="p-1.5 bg-gradient-to-tr from-purple-600 to-pink-500 rounded-xl text-white">
              <i data-lucide="sparkles" class="w-5 h-5"></i>
            </div>
            <div>
              <h3 class="font-bold text-sm text-gray-900 flex items-center space-x-1">
                <span>Gemini AI Assistant</span>
                <span class="bg-purple-100 text-purple-700 text-[10px] px-2 py-0.5 rounded-full font-bold">Pro</span>
              </h3>
              <p class="text-[10px] text-gray-500">Powered by Gemini 3 Flash</p>
            </div>
          </div>
          <button onclick="closeGeminiModal()" class="text-gray-400 hover:text-gray-600"><i data-lucide="x" class="w-5 h-5"></i></button>
        </div>

        <!-- AI Prompt Quick Suggestions -->
        <div class="py-2.5 flex space-x-2 overflow-x-auto no-scrollbar border-b text-[11px]">
          <button onclick="fillGeminiPrompt('Write a viral caption for a nature travel post with emojis and hashtags')" class="bg-purple-50 text-purple-700 font-medium px-3 py-1.5 rounded-full whitespace-nowrap hover:bg-purple-100 transition">🌿 Travel Caption</button>
          <button onclick="fillGeminiPrompt('Suggest 5 engaging Instagram story questions for my followers')" class="bg-pink-50 text-pink-700 font-medium px-3 py-1.5 rounded-full whitespace-nowrap hover:bg-pink-100 transition">💬 Story Ideas</button>
          <button onclick="fillGeminiPrompt('How can I gain more authentic followers on Instagram in 2026?')" class="bg-amber-50 text-amber-700 font-medium px-3 py-1.5 rounded-full whitespace-nowrap hover:bg-amber-100 transition">📈 Growth Strategy</button>
        </div>

        <!-- Chat Display Output -->
        <div id="gemini-output" class="flex-1 overflow-y-auto my-3 space-y-3 text-xs pr-1">
          <div class="bg-gradient-to-r from-purple-50 to-pink-50 p-3 rounded-2xl border border-purple-100 text-gray-800">
            👋 <strong>Hi Alex!</strong> I'm your Gemini AI Instagram Creator Assistant. Ask me to write captions, suggest reel ideas, optimize hashtags, or generate smart responses!
          </div>
        </div>

        <!-- Input Box -->
        <div class="border-t pt-2 flex items-center space-x-2">
          <input id="gemini-input" type="text" placeholder="Ask Gemini AI..." class="flex-1 bg-gray-100 rounded-full px-4 py-2.5 text-xs outline-none focus:ring-2 focus:ring-purple-400" onkeydown="if(event.key==='Enter') sendGeminiQuery()" />
          <button id="gemini-send-btn" onclick="sendGeminiQuery()" class="p-2.5 bg-gradient-to-tr from-purple-600 to-pink-500 text-white rounded-full hover:opacity-90 active:scale-95 transition">
            <i data-lucide="send" class="w-4 h-4"></i>
          </button>
        </div>
      </div>
    </div>

    <!-- 1. FULLSCREEN STORY VIEWER (With Swiping/Tapping) -->
    <div id="story-modal" class="hidden fixed inset-0 z-50 bg-black flex flex-col justify-between transition-opacity duration-300">
      <div class="p-3 z-10 space-y-2 bg-gradient-to-b from-black/70 to-transparent">
        <div class="flex space-x-1 w-full" id="story-progress-container">
          <div class="h-0.5 bg-white/40 flex-1 rounded-full overflow-hidden">
            <div id="story-progress-bar" class="h-full bg-white w-0"></div>
          </div>
        </div>
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-2">
            <img id="story-modal-avatar" class="w-8 h-8 rounded-full border border-white/40 object-cover" />
            <span id="story-modal-username" class="text-white text-xs font-semibold drop-shadow"></span>
            <span id="story-modal-time" class="text-white/60 text-xs">2h</span>
          </div>
          <button onclick="closeStoryModal()" class="text-white">
            <i data-lucide="x" class="w-6 h-6"></i>
          </button>
        </div>
      </div>

      <!-- Story Center Content Image with Touch Zones -->
      <div class="relative flex-1 flex items-center justify-center overflow-hidden">
        <!-- Left click/touch zone for previous story -->
        <div class="absolute left-0 top-0 bottom-0 w-1/3 z-20" onclick="prevStory()"></div>
        <!-- Right click/touch zone for next story -->
        <div class="absolute right-0 top-0 bottom-0 w-2/3 z-20" onclick="nextStory()"></div>
        <img id="story-modal-img" class="w-full h-full object-cover" />
      </div>

      <!-- Story Reply Footer Input -->
      <div class="p-4 z-10 flex items-center space-x-3 bg-gradient-to-t from-black/80 to-transparent">
        <input type="text" placeholder="Send message..." class="flex-1 bg-transparent border border-white/40 rounded-full px-4 py-2 text-xs text-white placeholder-white/70 outline-none backdrop-blur-sm" />
        <button class="text-white"><i data-lucide="heart" class="w-6 h-6"></i></button>
        <button class="text-white"><i data-lucide="send" class="w-6 h-6"></i></button>
      </div>
    </div>

    <!-- 2. CREATE POST MODAL WITH AI CAPTION GENERATOR -->
    <div id="create-modal" class="hidden fixed inset-0 z-50 bg-black/60 backdrop-blur-sm flex items-end md:items-center justify-center">
      <div class="bg-white w-full max-w-md rounded-t-2xl md:rounded-2xl p-4 space-y-4 animate-in slide-in-from-bottom duration-200">
        <div class="flex justify-between items-center border-b pb-2">
          <h3 class="font-bold text-center flex-1">Create New Post</h3>
          <button onclick="closeCreatePost()" class="text-gray-500"><i data-lucide="x" class="w-5 h-5"></i></button>
        </div>
        <div class="space-y-3">
          <label class="block text-xs font-semibold text-gray-500">Image URL</label>
          <input id="new-post-url" type="text" placeholder="Paste image URL..." value="https://images.unsplash.com/photo-1543466835-00a7907e9de1?auto=format&fit=crop&w=800&q=80" class="w-full border border-gray-200 rounded-lg p-2.5 text-xs outline-none focus:border-black" />
          
          <div class="flex justify-between items-center">
            <label class="block text-xs font-semibold text-gray-500">Caption</label>
            <button onclick="generateAICaptionForPost()" class="text-[11px] font-semibold text-purple-600 flex items-center space-x-1 hover:text-purple-700">
              <i data-lucide="sparkles" class="w-3.5 h-3.5"></i>
              <span>✨ AI Generate Caption</span>
            </button>
          </div>
          <textarea id="new-post-caption" rows="3" placeholder="Write a caption..." class="w-full border border-gray-200 rounded-lg p-2.5 text-xs outline-none focus:border-black">Loving the vibe today! ✨🐕</textarea>
          
          <button onclick="submitNewPost()" class="w-full bg-blue-500 text-white font-semibold py-2.5 rounded-lg text-sm hover:bg-blue-600 transition">Share Post</button>
        </div>
      </div>
    </div>

    <!-- 3. COMMENTS DRAWER -->
    <div id="comments-modal" class="hidden fixed inset-0 z-50 bg-black/50 flex items-end">
      <div class="bg-white w-full h-[65%] rounded-t-2xl flex flex-col p-4 animate-in slide-in-from-bottom duration-200">
        <div class="w-12 h-1 bg-gray-300 rounded-full mx-auto mb-3"></div>
        <div class="flex justify-between items-center border-b pb-2 mb-3">
          <h4 class="font-bold text-center flex-1 text-sm">Comments</h4>
          <button onclick="closeComments()" class="text-gray-500"><i data-lucide="x" class="w-5 h-5"></i></button>
        </div>
        <div class="flex-1 overflow-y-auto space-y-4 text-xs" id="comments-list">
          <!-- Dynamically populated -->
        </div>
        <div class="border-t pt-2 mt-2 flex items-center space-x-2">
          <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=100&q=80" class="w-7 h-7 rounded-full object-cover" />
          <input id="comment-input" type="text" placeholder="Add a comment..." class="flex-1 bg-gray-100 rounded-full px-3 py-2 text-xs outline-none" onkeydown="if(event.key==='Enter') postComment()" />
          <button onclick="postComment()" class="text-blue-500 font-semibold text-xs pr-1">Post</button>
        </div>
      </div>
    </div>

    <!-- 4. NOTIFICATIONS DRAWER -->
    <div id="notif-modal" class="hidden fixed inset-0 z-50 bg-black/50 flex justify-end">
      <div class="bg-white w-full max-w-xs h-full p-4 flex flex-col space-y-4 animate-in slide-in-from-right duration-200">
        <div class="flex items-center justify-between border-b pb-3">
          <h3 class="font-bold text-base">Notifications</h3>
          <button onclick="closeNotifications()"><i data-lucide="x" class="w-5 h-5"></i></button>
        </div>
        <div class="space-y-4 overflow-y-auto text-xs flex-1">
          <div class="flex items-center justify-between space-x-2">
            <div class="flex items-center space-x-2">
              <img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=100&q=80" class="w-9 h-9 rounded-full object-cover" />
              <div><span class="font-semibold">aimi.allover</span> liked your post. <span class="text-gray-400">2m</span></div>
            </div>
            <img src="https://images.unsplash.com/photo-1543466835-00a7907e9de1?auto=format&fit=crop&w=100&q=80" class="w-8 h-8 rounded object-cover" />
          </div>
          <div class="flex items-center justify-between space-x-2">
            <div class="flex items-center space-x-2">
              <img src="https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?auto=format&fit=crop&w=100&q=80" class="w-9 h-9 rounded-full object-cover" />
              <div><span class="font-semibold">ll_wyatt838</span> started following you. <span class="text-gray-400">1h</span></div>
            </div>
            <button class="bg-blue-500 text-white font-semibold px-3 py-1 rounded-lg text-[11px]">Follow</button>
          </div>
        </div>
      </div>
    </div>

    <!-- CUSTOM TOAST NOTIFICATION CONTAINER -->
    <div id="toast-container" class="fixed top-12 left-1/2 -translate-x-1/2 z-50 pointer-events-none flex flex-col items-center space-y-2 w-full max-w-xs px-4"></div>

  </div>

  <!-- JAVASCRIPT APPLICATION LOGIC -->
  <script>
    
    // REELS DATA FOR SWIPE FUNCTIONALITY
    const reelsData = [
      {
        id: 1,
        username: "nature_journey",
        avatar: "https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=100&q=80",
        img: "https://images.unsplash.com/photo-1518709268805-4e9042af9f23?auto=format&fit=crop&w=800&q=80",
        caption: "Golden hour walk through scenic trails 🌲✨ #nature #sunset #wanderlust",
        audio: "Original Audio - nature_journey",
        likes: "248K",
        comments: "1,420",
        shares: "18.2K"
      },
      {
        id: 2,
        username: "ocean_vibes",
        avatar: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=100&q=80",
        img: "https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=800&q=80",
        caption: "Surfing down the tropical coast lines! 🌊🏄‍♂️ #sea #surfing #tropics",
        audio: "Trending Audio - Ocean Waves",
        likes: "512K",
        comments: "3,890",
        shares: "42.1K"
      },
      {
        id: 3,
        username: "urban_streetart",
        avatar: "https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?auto=format&fit=crop&w=100&q=80",
        img: "https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?auto=format&fit=crop&w=800&q=80",
        caption: "Tokyo nights neon aesthetic vibes 🌃🏽 #tokyo #japan #nightlife",
        audio: "Synthwave Beats Vol. 4",
        likes: "189K",
        comments: "942",
        shares: "9.5K"
      }
    ];

    let currentReelIndex = 0;
    let reelTouchStartY = 0;

    // STORIES DATA
    const stories = [
      {
        id: 1,
        username: "aimi.allover",
        avatar: "https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=200&q=80",
        storyImg: "https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=800&q=80",
        hasUnseen: true
      },
      {
        id: 2,
        username: "ll_wyatt838",
        avatar: "https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?auto=format&fit=crop&w=200&q=80",
        storyImg: "https://images.unsplash.com/photo-1470071459604-3b5ec3a7fe05?auto=format&fit=crop&w=800&q=80",
        hasUnseen: true
      },
      {
        id: 3,
        username: "mia_creates",
        avatar: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&w=200&q=80",
        storyImg: "https://images.unsplash.com/photo-1519681393784-d120267933ba?auto=format&fit=crop&w=800&q=80",
        hasUnseen: true
      },
      {
        id: 4,
        username: "travel_ben",
        avatar: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=200&q=80",
        storyImg: "https://images.unsplash.com/photo-1476514525535-ce74f45814e0?auto=format&fit=crop&w=800&q=80",
        hasUnseen: false
      }
    ];

    // POSTS DATA
    let posts = [
      {
        id: 101,
        username: "alex.anyways18",
        userAvatar: "https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=200&q=80",
        location: "Central Park Walkways",
        postImg: "https://images.unsplash.com/photo-1543466835-00a7907e9de1?auto=format&fit=crop&w=800&q=80",
        likes: 1240,
        likedByText: "Liked by <span class='font-semibold'>aimi.allover</span> and <span class='font-semibold'>others</span>",
        likedAvatars: [
          "https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=100&q=80",
          "https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?auto=format&fit=crop&w=100&q=80"
        ],
        caption: "Thrilled to have directed my mega weekend walk with this little energetic buddy! 🐶☀️",
        commentsCount: 42,
        timeAgo: "2 hours ago",
        isLiked: false,
        isSaved: false,
        isFavorite: true,
        isFollowing: true,
        isMetaVerified: true
      },
      {
        id: 102,
        username: "aimi.allover",
        userAvatar: "https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=200&q=80",
        location: "Kyoto, Japan",
        postImg: "https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?auto=format&fit=crop&w=800&q=80",
        likes: 3890,
        likedByText: "Liked by <span class='font-semibold'>alex.anyways18</span> and <span class='font-semibold'>3,889 others</span>",
        likedAvatars: [
          "https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=100&q=80"
        ],
        caption: "Cherry blossoms in full bloom this morning. Beyond magical ✨🌸",
        commentsCount: 88,
        timeAgo: "5 hours ago",
        isLiked: true,
        isSaved: true,
        isFavorite: true,
        isFollowing: true,
        isMetaVerified: true
      }
    ];

    let commentsData = {
      101: [
        { name: "aimi.allover", text: "He looks so proud of himself!! ❤️" },
        { name: "ll_wyatt838", text: "Best director in town hands down 🐾" }
      ]
    };

    let activeFilter = 'all';
    let currentStoryIndex = 0;
    let storyTimer = null;

    // INITIALIZATION ON PAGE LOAD
    window.onload = function() {
      renderStories();
      renderPosts();
      renderExploreGrid();
      renderProfileGrid();
      renderReels();
      lucide.createIcons();

      // Hide Splash Screen after delay
      setTimeout(() => {
        const splash = document.getElementById('splash-screen');
        if (splash) {
          splash.classList.add('opacity-0');
          setTimeout(() => {
            splash.classList.add('hidden');
          }, 500);
        }
      }, 1600);
    };

    // TOAST DISPLAY HELPER
    function showToast(message) {
      const container = document.getElementById('toast-container');
      const toast = document.createElement('div');
      toast.className = "bg-gray-900/90 text-white text-xs px-4 py-2.5 rounded-full shadow-lg backdrop-blur-md border border-white/10 animate-in fade-in slide-in-from-top duration-200 pointer-events-auto flex items-center space-x-2";
      toast.innerHTML = `<i data-lucide="check-circle" class="w-4 h-4 text-green-400"></i><span>${message}</span>`;
      container.appendChild(toast);
      lucide.createIcons();
      setTimeout(() => {
        toast.classList.add('opacity-0', 'transition-opacity');
        setTimeout(() => toast.remove(), 300);
      }, 2500);
    }

    // GEMINI AI INTEGRATION API CALL
    async function callGeminiAPI(promptText) {
      const apiKey = "";
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent?key=${apiKey}`;
      const systemPrompt = "You are an expert Instagram social media consultant, viral content creator, and friendly AI assistant. Give concise, engaging, and well-formatted answers with appropriate emojis.";

      const payload = {
        contents: [{ parts: [{ text: promptText }] }],
        systemInstruction: { parts: [{ text: systemPrompt }] }
      };

      let attempts = 0;
      while (attempts < 3) {
        try {
          const response = await fetch(apiUrl, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(payload)
          });
          const data = await response.json();
          const reply = data.candidates?.[0]?.content?.parts?.[0]?.text;
          if (reply) return reply;
        } catch (e) {
          console.error("Gemini API call error:", e);
        }
        attempts++;
        await new Promise(r => setTimeout(r, 1000 * Math.pow(2, attempts)));
      }
      return "Gemini AI is currently busy. Please try again in a moment!";
    }

    function openGeminiModal() {
      document.getElementById('gemini-modal').classList.remove('hidden');
    }

    function closeGeminiModal() {
      document.getElementById('gemini-modal').classList.add('hidden');
    }

    function fillGeminiPrompt(text) {
      document.getElementById('gemini-input').value = text;
      sendGeminiQuery();
    }

    async function sendGeminiQuery() {
      const input = document.getElementById('gemini-input');
      const text = input.value.trim();
      if (!text) return;

      const output = document.getElementById('gemini-output');

      // Append User message
      output.innerHTML += `
        <div class="flex justify-end">
          <div class="bg-purple-600 text-white p-2.5 rounded-2xl max-w-[85%] leading-relaxed">${text}</div>
        </div>
      `;
      input.value = '';
      output.scrollTop = output.scrollHeight;

      // Append Loading indicator
      const loadingId = 'loading-' + Date.now();
      output.innerHTML += `
        <div id="${loadingId}" class="flex justify-start">
          <div class="bg-gray-100 text-gray-500 p-2.5 rounded-2xl flex items-center space-x-2">
            <i data-lucide="loader-2" class="w-4 h-4 animate-spin text-purple-600"></i>
            <span>Gemini AI is thinking...</span>
          </div>
        </div>
      `;
      lucide.createIcons();
      output.scrollTop = output.scrollHeight;

      // Call API
      const aiReply = await callGeminiAPI(text);

      const loadingEl = document.getElementById(loadingId);
      if (loadingEl) loadingEl.remove();

      // Append AI Response
      output.innerHTML += `
        <div class="flex justify-start">
          <div class="bg-gray-100 text-gray-800 p-3 rounded-2xl max-w-[90%] leading-relaxed whitespace-pre-line border border-gray-200/80">
            ${aiReply}
          </div>
        </div>
      `;
      output.scrollTop = output.scrollHeight;
      lucide.createIcons();
    }

    async function generateAICaptionForPost() {
      const captionTextarea = document.getElementById('new-post-caption');
      captionTextarea.value = "Generating AI caption with Gemini...";
      
      const prompt = "Generate a short, viral, aesthetic Instagram caption with emojis and relevant hashtags for a photo post.";
      const caption = await callGeminiAPI(prompt);
      captionTextarea.value = caption;
      showToast("Generated AI Caption!");
    }

    // REELS RENDER & SWIPE LOGIC
    function renderReels() {
      const wrapper = document.getElementById('reels-wrapper');
      
      wrapper.innerHTML = reelsData.map((r, i) => `
        <div class="w-full h-full absolute inset-0 transition-transform duration-300" style="transform: translateY(${(i - currentReelIndex) * 100}%);">
          <img src="${r.img}" class="w-full h-full object-cover opacity-90" />
          
          <!-- Play Overlay Button -->
          <button onclick="toggleReelPlay(this)" class="absolute inset-0 flex items-center justify-center bg-black/10 group">
            <div class="p-4 bg-black/40 rounded-full backdrop-blur-md opacity-0 group-hover:opacity-100 transition">
              <i data-lucide="pause" class="w-8 h-8 fill-white text-white"></i>
            </div>
          </button>

          <!-- Right Action Bar -->
          <div class="absolute right-3 bottom-20 flex flex-col items-center space-y-6 z-10">
            <button onclick="toggleReelLike(this)" class="flex flex-col items-center group">
              <i data-lucide="heart" class="w-7 h-7 stroke-[2] drop-shadow-lg group-hover:scale-110 transition"></i>
              <span class="text-xs font-semibold mt-1">${r.likes}</span>
            </button>
            <button onclick="openComments(101)" class="flex flex-col items-center group">
              <i data-lucide="message-circle" class="w-7 h-7 stroke-[2] drop-shadow-lg group-hover:scale-110 transition"></i>
              <span class="text-xs font-semibold mt-1">${r.comments}</span>
            </button>
            <button onclick="sharePost('${r.username}')" class="flex flex-col items-center group">
              <i data-lucide="send" class="w-7 h-7 stroke-[2] drop-shadow-lg group-hover:scale-110 transition"></i>
              <span class="text-xs font-semibold mt-1">${r.shares}</span>
            </button>
            <div class="w-8 h-8 rounded-lg overflow-hidden border-2 border-white mt-2">
              <img src="${r.avatar}" class="w-full h-full object-cover" />
            </div>
          </div>

          <!-- Bottom Left Overlay Info -->
          <div class="absolute bottom-20 left-4 z-10 max-w-[75%] space-y-2">
            <div class="flex items-center space-x-2">
              <img src="${r.avatar}" class="w-9 h-9 rounded-full border border-white/40 object-cover" />
              <span class="font-semibold text-sm drop-shadow">${r.username}</span>
              <button class="border border-white/60 text-xs px-2.5 py-1 rounded-lg font-semibold backdrop-blur-sm hover:bg-white/20 transition">Follow</button>
            </div>
            <p class="text-sm drop-shadow line-clamp-2">${r.caption}</p>
            <div class="flex items-center space-x-2 text-xs text-gray-200">
              <i data-lucide="music" class="w-3.5 h-3.5 animate-spin"></i>
              <span class="truncate">${r.audio}</span>
            </div>
          </div>
        </div>
      `).join('');

      lucide.createIcons();
    }

    function handleReelTouchStart(e) {
      reelTouchStartY = e.touches[0].clientY;
    }

    function handleReelTouchEnd(e) {
      const touchEndY = e.changedTouches[0].clientY;
      const diffY = reelTouchStartY - touchEndY;

      if (diffY > 50) {
        nextReel();
      } else if (diffY < -50) {
        prevReel();
      }
    }

    function nextReel() {
      if (currentReelIndex < reelsData.length - 1) {
        currentReelIndex++;
        renderReels();
      } else {
        currentReelIndex = 0; // loop back
        renderReels();
      }
    }

    function prevReel() {
      if (currentReelIndex > 0) {
        currentReelIndex--;
        renderReels();
      }
    }

    function toggleReelPlay(btn) {
      showToast("Paused reel playback");
    }

    function toggleReelLike(btn) {
      const heart = btn.querySelector('svg');
      heart.classList.toggle('text-red-500');
      heart.classList.toggle('fill-red-500');
    }

    // RENDER STORIES
    function renderStories() {
      const container = document.getElementById('stories-container');
      const storyElements = stories.map((s, idx) => `
        <div class="flex flex-col items-center space-y-1 flex-shrink-0 cursor-pointer" onclick="openStoryModal(${idx})">
          <div class="${s.hasUnseen ? 'story-ring' : 'story-ring-seen'} w-[68px] h-[68px] flex items-center justify-center">
            <img src="${s.avatar}" class="w-[62px] h-[62px] rounded-full object-cover border-2 border-white" />
          </div>
          <span class="text-[11px] text-gray-700 tracking-tight font-normal max-w-[68px] truncate">${s.username}</span>
        </div>
      `).join('');
      
      container.innerHTML = `
        <div class="flex flex-col items-center space-y-1 flex-shrink-0 cursor-pointer" onclick="openCreatePost()">
          <div class="relative w-[68px] h-[68px]">
            <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=200&q=80" class="w-full h-full rounded-full object-cover border border-gray-200" />
            <div class="absolute bottom-0 right-0 bg-blue-500 text-white p-0.5 rounded-full border-2 border-white">
              <i data-lucide="plus" class="w-3.5 h-3.5 stroke-[3]"></i>
            </div>
          </div>
          <span class="text-[11px] text-gray-600 tracking-tight">Your story</span>
        </div>
      ` + storyElements;
      lucide.createIcons();
    }

    // RENDER FEED POSTS
    function renderPosts() {
      const feed = document.getElementById('posts-feed');
      
      let filteredPosts = posts;
      if (activeFilter === 'following') filteredPosts = posts.filter(p => p.isFollowing);
      else if (activeFilter === 'favorites') filteredPosts = posts.filter(p => p.isFavorite);
      else if (activeFilter === 'meta_verified') filteredPosts = posts.filter(p => p.isMetaVerified);

      if (filteredPosts.length === 0) {
        feed.innerHTML = `
          <div class="py-16 text-center text-gray-400">
            <i data-lucide="sparkles" class="w-10 h-10 mx-auto mb-2 text-gray-300"></i>
            <p class="text-sm font-medium">No posts found in this filter.</p>
          </div>
        `;
        lucide.createIcons();
        return;
      }

      feed.innerHTML = filteredPosts.map(post => `
        <article class="bg-white pb-3">
          <div class="flex items-center justify-between px-3 py-2.5">
            <div class="flex items-center space-x-2.5">
              <div class="story-ring p-[1.5px]">
                <img src="${post.userAvatar}" class="w-8 h-8 rounded-full object-cover border border-white" />
              </div>
              <div class="flex flex-col">
                <div class="flex items-center space-x-1">
                  <span class="font-semibold text-xs text-gray-900">${post.username}</span>
                  ${post.isMetaVerified ? `<i data-lucide="badge-check" class="w-3.5 h-3.5 text-blue-500 fill-blue-500/10 inline"></i>` : ''}
                </div>
                ${post.location ? `<span class="text-[10px] text-gray-500 font-normal">${post.location}</span>` : ''}
              </div>
            </div>
            <button class="text-gray-700 active:opacity-50">
              <i data-lucide="more-horizontal" class="w-5 h-5"></i>
            </button>
          </div>

          <div class="relative w-full bg-gray-100 overflow-hidden cursor-pointer" ondoubleclick="handleDoubleTapLike(${post.id})">
            <img src="${post.postImg}" class="w-full h-auto max-h-[500px] object-cover block" alt="Post content" />
            <div id="heart-pop-${post.id}" class="absolute inset-0 flex items-center justify-center pointer-events-none opacity-0">
              <i data-lucide="heart" class="w-24 h-24 text-white fill-white drop-shadow-2xl"></i>
            </div>
          </div>

          <div class="px-3 pt-2.5 pb-1 flex justify-between items-center">
            <div class="flex items-center space-x-4">
              <button onclick="toggleLike(${post.id})" class="focus:outline-none active:scale-125 transition duration-150">
                <i data-lucide="heart" class="w-6 h-6 ${post.isLiked ? 'text-red-500 fill-red-500 stroke-red-500' : 'text-black stroke-[1.8]'}"></i>
              </button>
              <button onclick="openComments(${post.id})" class="focus:outline-none active:scale-110 transition">
                <i data-lucide="message-circle" class="w-6 h-6 text-black stroke-[1.8] -rotate-12"></i>
              </button>
              <button onclick="sharePost('${post.username}')" class="focus:outline-none active:scale-110 transition">
                <i data-lucide="send" class="w-6 h-6 text-black stroke-[1.8]"></i>
              </button>
            </div>
            <button onclick="toggleSave(${post.id})" class="focus:outline-none active:scale-110 transition">
              <i data-lucide="bookmark" class="w-6 h-6 ${post.isSaved ? 'text-black fill-black' : 'text-black stroke-[1.8]'}"></i>
            </button>
          </div>

          <div class="px-3 py-1">
            <div class="flex items-center space-x-2">
              ${post.likedAvatars && post.likedAvatars.length > 0 ? `
                <div class="flex -space-x-1.5 items-center">
                  ${post.likedAvatars.map(av => `<img src="${av}" class="w-4 h-4 rounded-full border border-white object-cover" />`).join('')}
                </div>
              ` : ''}
              <p class="text-xs text-gray-900 leading-tight">${post.likedByText}</p>
            </div>
          </div>

          <div class="px-3 pt-0.5 text-xs text-gray-900 space-x-1 leading-snug">
            <span class="font-semibold">${post.username}</span>
            <span>${post.caption}</span>
          </div>

          <div class="px-3 pt-1">
            <button onclick="openComments(${post.id})" class="text-xs text-gray-400 font-normal hover:underline">
              View all ${post.commentsCount} comments
            </button>
          </div>

          <div class="px-3 pt-0.5">
            <span class="text-[10px] text-gray-400 uppercase tracking-tight">${post.timeAgo}</span>
          </div>
        </article>
      `).join('');

      lucide.createIcons();
    }

    // DROPDOWN FILTER LOGIC
    function toggleDropdown() {
      const menu = document.getElementById('dropdown-menu');
      const chevron = document.getElementById('dropdown-chevron');
      if (menu.classList.contains('hidden')) {
        menu.classList.remove('hidden');
        setTimeout(() => {
          menu.classList.remove('opacity-0', 'scale-95');
          chevron.classList.add('rotate-180');
        }, 10);
      } else {
        menu.classList.add('opacity-0', 'scale-95');
        chevron.classList.remove('rotate-180');
        setTimeout(() => menu.classList.add('hidden'), 200);
      }
    }

    function filterFeed(filterType) {
      activeFilter = filterType;
      toggleDropdown();
      
      const banner = document.getElementById('filter-banner');
      const bannerText = document.getElementById('filter-banner-text');

      if (filterType === 'all') {
        banner.classList.add('hidden');
      } else {
        banner.classList.remove('hidden');
        if (filterType === 'following') bannerText.innerText = "Showing: Following";
        if (filterType === 'favorites') bannerText.innerText = "Showing: Favorites";
        if (filterType === 'meta_verified') bannerText.innerText = "Showing: Meta Verified";
      }
      renderPosts();
    }

    // LIKES AND SAVES
    function toggleLike(postId) {
      const post = posts.find(p => p.id === postId);
      if (post) {
        post.isLiked = !post.isLiked;
        post.likes += post.isLiked ? 1 : -1;
        renderPosts();
      }
    }

    function handleDoubleTapLike(postId) {
      const post = posts.find(p => p.id === postId);
      if (post) {
        post.isLiked = true;
        const heartEl = document.getElementById(`heart-pop-${postId}`);
        if (heartEl) {
          heartEl.classList.remove('opacity-0');
          heartEl.classList.add('animate-heart-pop');
          setTimeout(() => {
            heartEl.classList.remove('animate-heart-pop');
            heartEl.classList.add('opacity-0');
          }, 800);
        }
        renderPosts();
      }
    }

    function toggleSave(postId) {
      const post = posts.find(p => p.id === postId);
      if (post) {
        post.isSaved = !post.isSaved;
        showToast(post.isSaved ? "Saved to your collection" : "Removed from saved");
        renderPosts();
      }
    }

    // TAB SWITCHING
    function switchTab(tabName) {
      document.querySelectorAll('.page-view').forEach(view => view.classList.add('hidden'));
      document.getElementById('main-header').classList.remove('hidden');

      if (tabName === 'home') document.getElementById('view-home').classList.remove('hidden');
      else if (tabName === 'search') document.getElementById('view-search').classList.remove('hidden');
      else if (tabName === 'reels') {
        document.getElementById('view-reels').classList.remove('hidden');
        document.getElementById('main-header').classList.add('hidden');
      } else if (tabName === 'profile') document.getElementById('view-profile').classList.remove('hidden');
      
      lucide.createIcons();
    }

    // STORY NAVIGATION
    function openStoryModal(index) {
      currentStoryIndex = index;
      const s = stories[index];
      s.hasUnseen = false;
      renderStories();

      document.getElementById('story-modal-avatar').src = s.avatar;
      document.getElementById('story-modal-username').innerText = s.username;
      document.getElementById('story-modal-img').src = s.storyImg;

      const modal = document.getElementById('story-modal');
      modal.classList.remove('hidden');

      const bar = document.getElementById('story-progress-bar');
      bar.classList.remove('animate-progress');
      void bar.offsetWidth;
      bar.classList.add('animate-progress');

      clearTimeout(storyTimer);
      storyTimer = setTimeout(() => nextStory(), 5000);
    }

    function nextStory() {
      if (currentStoryIndex < stories.length - 1) {
        openStoryModal(currentStoryIndex + 1);
      } else {
        closeStoryModal();
      }
    }

    function prevStory() {
      if (currentStoryIndex > 0) {
        openStoryModal(currentStoryIndex - 1);
      }
    }

    function closeStoryModal() {
      clearTimeout(storyTimer);
      document.getElementById('story-modal').classList.add('hidden');
    }

    // MODAL DIALOGS
    function openCreatePost() {
      document.getElementById('create-modal').classList.remove('hidden');
    }

    function closeCreatePost() {
      document.getElementById('create-modal').classList.add('hidden');
    }

    function submitNewPost() {
      const url = document.getElementById('new-post-url').value;
      const caption = document.getElementById('new-post-caption').value;
      if (!url) return;

      const newPost = {
        id: Date.now(),
        username: "alex.anyways18",
        userAvatar: "https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=200&q=80",
        location: "Just Now",
        postImg: url,
        likes: 1,
        likedByText: "Liked by <span class='font-semibold'>you</span>",
        likedAvatars: ["https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=100&q=80"],
        caption: caption,
        commentsCount: 0,
        timeAgo: "1 moment ago",
        isLiked: true,
        isSaved: false,
        isFavorite: true,
        isFollowing: true,
        isMetaVerified: true
      };

      posts.unshift(newPost);
      closeCreatePost();
      switchTab('home');
      renderPosts();
      renderProfileGrid();
      showToast("Post shared successfully!");
    }

    let activeCommentPostId = null;
    function openComments(postId) {
      activeCommentPostId = postId;
      const modal = document.getElementById('comments-modal');
      const list = document.getElementById('comments-list');
      
      const comments = commentsData[postId] || [
        { name: "aimi.allover", text: "Love this so much!" },
        { name: "ll_wyatt838", text: "Awesome picture ✨" }
      ];

      list.innerHTML = comments.map(c => `
        <div class="flex items-start justify-between space-x-2">
          <div class="flex items-start space-x-2">
            <img src="https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?auto=format&fit=crop&w=100&q=80" class="w-7 h-7 rounded-full object-cover" />
            <div>
              <span class="font-semibold text-gray-900">${c.name}</span>
              <span class="text-gray-700 ml-1">${c.text}</span>
              <div class="text-[10px] text-gray-400 mt-0.5 space-x-2">
                <span>1h</span>
                <button class="font-semibold">Reply</button>
              </div>
            </div>
          </div>
          <button class="text-gray-400"><i data-lucide="heart" class="w-3.5 h-3.5"></i></button>
        </div>
      `).join('');

      modal.classList.remove('hidden');
      lucide.createIcons();
    }

    function closeComments() {
      document.getElementById('comments-modal').classList.add('hidden');
    }

    function postComment() {
      const input = document.getElementById('comment-input');
      if (!input.value.trim() || !activeCommentPostId) return;

      if (!commentsData[activeCommentPostId]) commentsData[activeCommentPostId] = [];
      commentsData[activeCommentPostId].push({ name: "alex.anyways18", text: input.value });

      input.value = '';
      openComments(activeCommentPostId);

      const post = posts.find(p => p.id === activeCommentPostId);
      if (post) {
        post.commentsCount += 1;
        renderPosts();
      }
    }

    function openNotifications() {
      document.getElementById('notif-modal').classList.remove('hidden');
    }

    function closeNotifications() {
      document.getElementById('notif-modal').classList.add('hidden');
    }

    function openMessages() {
      showToast("Direct Messages opened!");
    }

    function sharePost(username) {
      document.execCommand('copy');
      showToast(`Link copied! Share ${username}'s post.`);
    }

    function renderExploreGrid() {
      const grid = document.getElementById('explore-grid');
      const imgs = [
        "https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=300&q=80",
        "https://images.unsplash.com/photo-1519681393784-d120267933ba?auto=format&fit=crop&w=300&q=80",
        "https://images.unsplash.com/photo-1470071459604-3b5ec3a7fe05?auto=format&fit=crop&w=300&q=80",
        "https://images.unsplash.com/photo-1543466835-00a7907e9de1?auto=format&fit=crop&w=300&q=80",
        "https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?auto=format&fit=crop&w=300&q=80",
        "https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=300&q=80"
      ];
      grid.innerHTML = imgs.map(src => `
        <div class="aspect-square bg-gray-200 overflow-hidden cursor-pointer hover:opacity-90">
          <img src="${src}" class="w-full h-full object-cover" />
        </div>
      `).join('');
    }

    function renderProfileGrid() {
      const grid = document.getElementById('profile-grid');
      grid.innerHTML = posts.map(p => `
        <div class="aspect-square bg-gray-200 overflow-hidden cursor-pointer relative group">
          <img src="${p.postImg}" class="w-full h-full object-cover" />
          <div class="absolute inset-0 bg-black/30 opacity-0 group-hover:opacity-100 flex items-center justify-center space-x-3 text-white text-xs font-bold transition">
            <span class="flex items-center space-x-1"><i data-lucide="heart" class="w-3.5 h-3.5 fill-white"></i> <span>${p.likes}</span></span>
          </div>
        </div>
      `).join('');
      lucide.createIcons();
    }
  </script>
</body>
</html>
